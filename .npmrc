# Enforce engine-strict to prevent using wrong Node.js version
engine-strict=true

# Use pnpm for lockfile consistency
package-manager-strict=true

# Auto install peers
auto-install-peers=true

# Shamefully hoist for compatibility with some packages
shamefully-hoist=false

